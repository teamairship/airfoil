# <%= props.title || Title %>

## Status

<%= props.status || "What is the status, such as `proposed`, `accepted`, `rejected`, `deprecated`, `superseded`, etc.?" %>

---

## Context

<%= props.context || "What is the issue that we're seeing that is motivating this decision or change?" %>

---

## Decision

<%= props.decision || "What is the change that we're proposing and/or doing?" %>

---

## Consequences

<%= props.consequences || "What becomes easier or more difficult to do because of this change?" %>
