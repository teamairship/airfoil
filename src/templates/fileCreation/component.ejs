import React, { FC } from 'react';
import { StyleSheet } from 'react-native';

type Props = {};

export const <%= props.fileName %>: FC<Props> = () => {
  return (
    <></>
  );
};

const styles = StyleSheet.create({});